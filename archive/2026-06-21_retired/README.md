# Archive — retired 2026-06-21

Frozen reference copies of things removed from the live app. **Not imported or
bundled** (this lives outside `src/`).

| Archived | What it was | Live route (now removed) |
|---|---|---|
| `HubPage.jsx` | Legacy "classic" logged-in hub | was `/hub/classic` |
| `little-architect/` | A game folder never in the registry (`games.js`) or main routes — only the classic hub referenced it | none (was only reachable via classic hub) |

Kept for reference/rollback. Safe to delete permanently once you're sure they're
not needed.
